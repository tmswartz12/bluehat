import styled from "styled-components";
import { Navbar, Container, NavDropdown, Nav, Badge } from "react-bootstrap";

export const LoggedOutNav = styled(Navbar)`
  position: absolute;
  z-index: 100;
  width: 100%;
`;
